export type Cabecalho1Props = {
    paginaProps:string;
    nrPaginaProps:number | string;
    children:React.ReactNode;
}
export type CabecalhoProps2 = {
    statusProps: "loading" | "deployed"
    avisoProps: Function;
}